// You can remove this file or keep it as reference
// But you will NOT need to import it anywhere for CRUD operations

export default function Book(id, title, author, price, isbn) {
  return { id, title, author, price, isbn };
}
